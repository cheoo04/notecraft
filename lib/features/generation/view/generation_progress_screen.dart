import 'package:flutter/material.dart';

/// Écran 4 — Traitement & progression asynchrone.
/// TODO: indicateur circulaire, étapes (transcription/vectorisation),
/// notification locale à la fin du mode Affiné.
class GenerationProgressScreen extends StatelessWidget {
  const GenerationProgressScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('TODO: progression du traitement')),
    );
  }
}
